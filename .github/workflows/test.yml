on:
  workflow_dispatch:
  pull_request:
jobs:
  test:
    environment: ${{ false && 'one' || 'two' }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo SECRET_ONE=$SECRET_ONE
          echo SECRET_TWO=$SECRET_TWO
          read
        env:
          SECRET_ONE: ${{ secrets.SECRET_TWO }}
          SECRET_TWO: ${{ secrets.SECRET_ONE }}
