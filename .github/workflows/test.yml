on: release
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        id: script
        with:
          script: return JSON.stringify(context);
          result-encoding: string
      - name: Get result
        run: printenv RESULT
        env:
          RESULT: ${{ steps.script.outputs.result }}
