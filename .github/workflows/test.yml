on: push
jobs:
  test:
    runs-on: ubuntu-latest
    container: ghcr.io/iterative/cml
    steps:
      - uses: actions/checkout@v2
      - run: |
          cml-send-comment --version || true
          cml-send-comment -v || true
          cml-send-comment -h

       
