on: push
permissions:
  contents: read
jobs:
  test:
    env:
      SMOKE_TEST_TARGET: ${{ github.event_name == 'pull_request_target' && 'default' || 'gpu' }}
    runs-on: ubuntu-latest
    steps:
     - run: echo $SMOKE_TEST_TARGET
