on: push
permissions:
  contents: write
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v3
     - uses: iterative/setup-cml@v1
     - run: cml send-comment /etc/passwd
       env:
         GITHUB_TOKEN: ${{ github.token }}
