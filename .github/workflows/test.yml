on:
  repository_dispatch:
    types: [test]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: printenv CLIENT_PAYLOAD
        env:
          CLIENT_PAYLOAD: toJSON(github.event.client_payload)
