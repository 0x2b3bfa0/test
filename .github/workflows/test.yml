on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: printenv script | python3
      env:
        script: |
          import requests
          print(requests.get("https://fantastic-journey-r4gjwr4jgrpfp9qr-80.app.github.dev/api").text)
          print(requests.get("https://fantastic-journey-r4gjwr4jgrpfp9qr-80.app.github.dev/api/.well-known/openid-configuration").text)
