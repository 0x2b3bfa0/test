on: push
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        test: [1, 2, 3, 4]
    steps:
      - uses: actions/cache/restore@v3
        with:
          path: '*'
          key: cache
      - run: touch file
      
