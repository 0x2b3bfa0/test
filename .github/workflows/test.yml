name: Release
on:
  release:
    types:
      - published
      - unpublished
      - created
      - edited
      - deleted
      - prereleased
      - released
jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
     - run: echo ${{ github.event_name }}
