on: push
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        file: [1,2,3,4]
    steps:
    - run: date > age-${{ matrix.file }}
    - uses: actions/upload-artifact@v3
      with:
        name: age-binaries
        path: age-*
