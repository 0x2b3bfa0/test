on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - id: count
        run: |
          echo -e "a=0\nb=1\nc=2\n" |
          tee "$GITHUB_OUTPUT"
      - name: Notify delible cloud resources
        if: steps.count.outputs.b > 0
        run: true
