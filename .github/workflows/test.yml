on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: sleep infinity
  continuation:
    needs: test
    if: success() || failure()
    runs-on: ubuntu-latest
    steps:
      - run: true
