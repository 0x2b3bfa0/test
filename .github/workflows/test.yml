on: push
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        secret:
          - ${{ secrets.A }}
          - ${{ secrets.SECRET }}
    steps:
      - run: printenv TEST
        env:
          TEST: ${{ matrix.secret }}
