on:
  release:
    types: [prereleased]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
     - run: gh release edit $TAG --prerelease=false
       env:
         GITHUB_TOKEN: ${{ github.token }}
         TAG: ${{ github.event.release.tag_name }}
