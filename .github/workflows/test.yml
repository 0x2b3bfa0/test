on: push
jobs:
  one:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        test: [1, 2, 3]
    steps:
    - run: echo ${{ matrix.test }}
  two:
    needs: one
    runs-on: ubuntu-latest
    steps:
    - run: printenv MATRIX
      env:
        MATRIX: ${{ toJSON(needs.one) }}
