on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: iterative/setup-cml@v3
      - run: date > file && date>a; git add file; git add a; date >> cml.yml; cml pr create .
        env:
          CML_TOKEN: ${{ github.token }}
