on: push
jobs:
  one:
    runs-on: ubuntu-latest
    steps:
    - run: true
  two:
    runs-on: ubuntu-latest
    steps:
    - run: false
  three:
    needs: [one, two]
    if: success() || failure()
    runs-on: ubuntu-latest
    steps:
    - run: echo ${{ needs.o*.result }}
