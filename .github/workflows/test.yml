on: push
jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - run: go install github.com/jwilder/dockerize@v0.6.1
      - run: dockerize
  two:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v3
      - run: go install github.com/jwilder/dockerize@v0.6.1
      - run: dockerize
