 on:
   pull_request:
     types: [closed]
 jobs:
   test:
      runs-on: ubuntu-latest
      steps:
      - run: echo "$A"
        env:
          A: ${{ github.event.pull_request.merge_commit_sha }}
