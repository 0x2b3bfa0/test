on:
  repository_dispatch:
    types: [test]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: printenv A && printenv B
        env:
          A: ${{ 'dev' == 'dev' && 'dev-' || '' }}
          B: ${{ 'foo' == 'dev' && 'dev-' || '' }}
