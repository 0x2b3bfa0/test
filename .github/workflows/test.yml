name: Release
on: release
jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
     - run: date>test
     - uses: softprops/action-gh-release@v1
       if: github.event_name == 'release'
       with:
         files: test
