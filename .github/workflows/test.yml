on:
  workflow_call:
    inputs:
      release:
        required: true
        type: boolean
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: printenv INPUTS
      env:
        INPUTS: ${{ toJSON(inputs) }}
