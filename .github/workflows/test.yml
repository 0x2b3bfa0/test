on: push
jobs:
  test:
    runs-on: ubuntu-latest
    if: false
    steps:
     - run: echo $SMOKE_TEST_TARGET
  test1:
    runs-on: ubuntu-latest
    needs: test
    steps:
     - run: echo $SMOKE_TEST_TARGET
