 on: push
 jobs:
   test:
      timeout-minutes: 60
      runs-on: ubuntu-latest
      steps:
      - run: sleep 10
   test1:
     timeout-minutes: 60
     runs-on: ubuntu-latest
     steps:
     - run: |
          while true; do echo $((++a))
            timeout 64 sleep $((2**++exponent)) # exponential backoff, maximum 64 seconds
          done
