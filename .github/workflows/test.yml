on: push
jobs:
  one:
    runs-on: ubuntu-latest
    steps:
    - run: true
  two:
    runs-on: ubuntu-latest
    steps:
    - run: false
  three:
    needs: [one, two]
    if: success() || (failure() && needs.one.result == 'success')
    runs-on: ubuntu-latest
    steps:
    - run: echo ${{ needs.*.result }}
