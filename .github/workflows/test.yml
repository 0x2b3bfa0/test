on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: date > age-${{ matrix.file }}
    - uses: actions/upload-artifact@v3
      with:
        name: age-binaries
        path: age-*
