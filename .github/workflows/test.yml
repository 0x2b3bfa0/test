on: push
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        part: [1, 2]
    steps:
      - uses: actions/cache/restore@v3
        with:
          path: file
          key: cache-*
      - run: cat file
        continue-on-error: true
      - run: date > file-${{ matrix.part }}
      - uses: actions/upload-artifact@v3
        with:
          name: artifact
          path: file-*
  collect:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/download-artifact@v3
        with:
          name: artifact
          path: .
      - run: cat file-* > file
      - uses: actions/cache/save@v3
        with:
          path: file
          key: cache-${{ hashFiles('file') }}
