on:
  workflow_dispatch:
    inputs:
      nuke:
        description: Delete cloud resources 💥
        type: boolean
        default: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo -- ${{ inputs.nuke && '--no-dry-run' || '' }}
