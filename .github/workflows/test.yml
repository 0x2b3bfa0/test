on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: sudo apt update && sudo apt install --yes snapcraft && echo "$PASSWORD" | snapcraft login --with -
      env:
        PASSWORD: ${{ secrets.PASSWORD }}
