on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - run: sudo apt update && sudo apt install --yes snapcraft && snapcraft login
      env:
        SNAPCRAFT_STORE_CREDENTIALS: ${{ secrets.PASSWORD }}
