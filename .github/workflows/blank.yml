on: push
jobs:
  fails:
    runs-on: macos-latest
    steps:
    - run: brew install dvc
    - run: dvc doctor && dvc metrics show
  succeeds:
    runs-on: macos-latest
    steps:
    - run: pip3 install dvc==2.10.0
    - run: dvc doctor && dvc metrics show
