on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: iterative/setup-cml@v1
    - run: |
        convert -size 100x100 -xc:red test.png
        cml-publish test.png
      env:
        REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
