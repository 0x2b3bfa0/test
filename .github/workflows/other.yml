on: push
jobs:
  ldid:
    runs-on: ubuntu-latest
    steps:
    - run: pip install conda && conda install ldid
  yes:
    uses: ./.github/workflows/test.yml
    with:
      release: true
  no:
    uses: ./.github/workflows/test.yml
    with:
      release: false
