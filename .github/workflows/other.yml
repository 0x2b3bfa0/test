on:
  repository_dispatch:
    types: [age-interop-request]
jobs:
  cml-pr:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.event.event_type }} ${{ github.event.client_playload.sha }}
      
