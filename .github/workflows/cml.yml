on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: iterative/setup-cml@v1
        with:
          ci: --unshallow --user-name=Helio
      - uses: iterative/setup-cml@v1
        with:
          ci: true
